<h2>My tasks</h2>
<ul>
<% @tasks.each do |task| %>
<li><%= link_to task.title, task_path(task) %> <%= link_to '<i class="fa-solid fa-trash"></i>'.html_safe, delete_task_path(task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></li>
<% end %>
</ul>
